import React from "react";


import {MenuListInterface} from "../interfaces/item.interface";

type ListType = {
    menuList: MenuListInterface[] | null
}
export const List = ({menuList}: ListType) => {
    return <></>
}